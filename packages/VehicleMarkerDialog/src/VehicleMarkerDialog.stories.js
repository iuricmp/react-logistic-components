import React from 'react';
import { storiesOf } from '@storybook/react'
import VehicleMarkerDialog from './VehicleMarkerDialog'

storiesOf('VehicleMarkerDialog', module)
  .add('default', () => <VehicleMarkerDialog />)
